# Database Configuration
POSTGRES_DB=veles_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

DATABASE_URL=postgres://postgres:postgres@localhost:5432/veles_dev
# For Docker containers - use 'db' hostname
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/veles_dev
VITE_TEST=test

# Note: For local development outside Docker, create .env.local with:

