---
name: install-dependencies
description: Use when adding or installing project dependencies; dependency versions must always be pinned exactly.
---

# Install Dependencies

Pin every dependency to an exact version when adding or installing it.

- Use the package manager's exact-version option when installing.
- Do not add version ranges (`^`, `~`), floating tags such as `latest`, or unversioned dependencies.
